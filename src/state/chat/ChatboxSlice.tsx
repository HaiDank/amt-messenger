import { createSlice } from "@reduxjs/toolkit";


type chatboxType = {

}

const initialState = {

}

export const chatboxSlice = createSlice({
    name: 'chatbox',
    initialState,
    reducers: {
        saveChatbox: (state, action) => {

        }
    }
})

export default chatboxSlice.reducer;